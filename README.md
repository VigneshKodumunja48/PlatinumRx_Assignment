PlatinumRx Data Analyst Assignment

SQL:
- Created tables and inserted sample data
- Solved queries using joins, aggregation, and window functions

Excel:
- Used INDEX-MATCH for lookup
- Used date functions to find same-day and same-hour tickets

Python:
- Converted minutes into readable format
- Removed duplicate characters from string

Note:
Sample data was created since dataset was not provided.